buildscript {
    repositories {
        jcenter()
    }
    dependencies {
        classpath 'com.android.tools.build:gradle:1.2.3'
    }
}

subprojects {
    repositories {
		mavenLocal()
        jcenter()
    }

	task wrapper(type: Wrapper) {
		gradleVersion = '2.4'
	}
}
