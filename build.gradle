buildscript {
    repositories {
        jcenter()
    }
    dependencies {
        classpath 'com.android.tools.build:gradle:1.1.0'
    }
}

subprojects {
    repositories {
		mavenLocal()
        jcenter()
    }

	task wrapper(type: Wrapper) {
		gradleVersion = '2.4'
	}
}
